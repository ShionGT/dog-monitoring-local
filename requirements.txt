# Core runtime dependencies (macOS dev + Raspberry Pi 5)
Flask>=3.0,<4.0
Werkzeug>=3.0,<4.0
Pillow>=10.0
requests>=2.28

# --- Raspberry Pi 5 hardware backends (only needed on the Pi) ---
# These are imported lazily inside the hardware/camera modules, so on a
# laptop or in CI (mock mode) they are never imported and the app runs fine.
gpiozero>=2.0
picamera2>=0.3.2

# --- Webcam support (macOS / Linux USB cameras via OpenCV) ---
# Imported lazily by the 'webcam' frame source; harmless if unused.
opencv-python>=4.8
